<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Cuenta regresiva – Mis 18</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #0f0f0f, #1a1a1a);
      color: white;
      text-align: center;
    }

    h1 {
      margin-top: 30px;
      font-size: 2.5em;
      letter-spacing: 2px;
    }

    .contador {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin: 30px 0;
      flex-wrap: wrap;
    }

    .box {
      background: rgba(255,255,255,0.1);
      padding: 15px 20px;
      border-radius: 12px;
      min-width: 80px;
    }

    .box span {
      font-size: 2em;
      display: block;
      font-weight: bold;
    }

    .box small {
      font-size: 0.8em;
      opacity: 0.8;
    }

    .galeria {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 15px;
      padding: 20px;
    }

    .galeria img {
      width: 100%;
      border-radius: 15px;
      object-fit: cover;
    }

    .aclaraciones {
      background: rgba(255,255,255,0.08);
      margin: 30px 20px;
      padding: 20px;
      border-radius: 15px;
    }

    .aclaraciones p {
      margin: 10px 0;
      font-size: 1em;
    }

    footer {
      margin: 30px 0;
      font-size: 0.9em;
      opacity: 0.7;
    }
  </style>
</head>

<body>

  <h1>✨ Cuenta regresiva para mis 18 ✨</h1>

  <div class="contador">
    <div class="box">
      <span id="dias">0</span>
      <small>Días</small>
    </div>
    <div class="box">
      <span id="horas">0</span>
      <small>Horas</small>
    </div>
    <div class="box">
      <span id="minutos">0</span>
      <small>Minutos</small>
    </div>
    <div class="box">
      <span id="segundos">0</span>
      <small>Segundos</small>
    </div>
  </div>

  <div class="galeria">
    <img src="foto1.jpg">
    <img src="foto2.jpg">
    <img src="foto3.jpg">
    <img src="foto4.jpg">
  </div>

  <div class="aclaraciones">
    <p>🎁 Si estás pensando en un regalo, lo que más me gustaría es <b>plata en efectivo</b></p>
    <p>💳 También podés usar mi alias:</p>
    <p><b>giuli.tol1</b></p>
  </div>

  <footer>
    🖤 Sábado 10 de enero 🖤
  </footer>

  <script>
    const fechaEvento = new Date("January 10, 2026 00:00:00").getTime();

    setInterval(() => {
      const ahora = new Date().getTime();
      const diferencia = fechaEvento - ahora;

      const dias = Math.floor(diferencia / (1000 * 60 * 60 * 24));
      const horas = Math.floor((diferencia / (1000 * 60 * 60)) % 24);
      const minutos = Math.floor((diferencia / (1000 * 60)) % 60);
      const segundos = Math.floor((diferencia / 1000) % 60);

      document.getElementById("dias").innerHTML = dias;
      document.getElementById("horas").innerHTML = horas;
      document.getElementById("minutos").innerHTML = minutos;
      document.getElementById("segundos").innerHTML = segundos;
    }, 1000);
  </script>

</body>
</html>
